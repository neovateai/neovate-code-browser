import { SenderContextTag } from './ContextTag';
import { SenderButton } from './SenderButton';
import { SenderSelect } from './SenderSelect';

export default {
  Button: SenderButton,
  Select: SenderSelect,
  ContextTag: SenderContextTag,
};
